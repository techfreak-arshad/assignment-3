#include<stdio.h>
int main()
{
int num;
printf("Enter the number\n");
scanf("%d",&num);
if(num>0)
printf("positive no");
else
if(num<0)
printf("non positive no");
else
printf("no is  zero");
return 0;
}

